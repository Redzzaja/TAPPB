import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.mealplanner.app",
  appName: "Meal Planner",
  webDir: "out", // <--- UBAH INI DARI 'public' MENJADI 'out'
  // ...
};

export default config;
